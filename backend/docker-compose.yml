# Prisma + MongoDB require a replica set for writes (including seed).
# After `docker compose up -d`, run the rs.initiate command in README once.
services:
  mongodb:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
